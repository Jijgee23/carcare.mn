CARCARE.MN — Хөгжүүлэлтийн түүх (Dev history)
================================================

Энэ файлд git commit-ийн хооронд хийгдсэн ажлуудыг он цагийн дарааллаар
(шинэ нь дээрээ) тэмдэглэнэ. Бичлэг бүр: огноо, суурь commit, хийсэн ажлууд.


------------------------------------------------------------------------
2026-06-25  ·  commit хийгдээгүй
------------------------------------------------------------------------

SUBSCRIPTION-ыг бүрэн ажиллагаатай болгов (anhааруулга, гэйт, API, routing)
--------------------------------------------------------------------------
Бодлого: багц (trial/төлбөртэй) дуусахад → read-only + banner, owner-ийг
subscription page руу авто чиглүүлнэ. Бүх багцад хамаарна (active subscription
байхгүй / endsAt бодит цагт өнгөрсөн бол locked).

- lib/subscription.ts: SUBSCRIPTION_WARN_DAYS (7), SUBSCRIPTION_LOCKED_MESSAGE
  тогтмолууд нэмэв.
- lib/subscription-server.ts (шинэ): getSubscriptionState (React cache, endsAt-
  aware → cron-оос үл хамаарч бодит цагт locked тооцно), assertActiveSubscription
  (action-д throw), requireActiveSubscriptionApi (API-д 403 + code
  SUBSCRIPTION_EXPIRED), getAppBaseUrl.
- lib/subscription-payments.ts (шинэ): confirmSubscriptionPayment(paymentId) —
  төлбөр баталгаажуулах core (хуучин checkSubscriptionPaymentAction-аас гаргаж
  авав). Web polling + QPay callback хоёул дуудна. QPay.checkPayment-ээр бие
  даан баталгаажуулдаг тул callback-ийг хуурамчаар дуудаж болохгүй.

Анхааруулга / routing:
- app/_components/subscription-banner.tsx (шинэ): locked / trial / expiringSoon /
  pending payment төлвүүдийг харуулна (хуучин trial-banner.tsx-ийг орлов, тэр
  файлыг устгав).
- app/_components/subscription-guard.tsx (шинэ, client): locked үед OWNER-ийг
  /dashboard/settings/subscription руу router.replace хийнэ (ажилтныг үгүй).
- app/dashboard/layout.tsx: getSubscriptionState ашиглаж banner + guard render.

Action хязгаарлалт (read-only enforcement) — authorize()-д assertActiveSubscription
нэмэв: orders, customers, vehicles, services, branches, employees,
diagnostic-templates, quick-create, appointments(authorizeStaff). Owner-ийн
тохиргооны action-ууд (tenant, units, labor-categories, roles, qpay,
subscription-payments) гэйтгүй — owner төлбөр төлж чадахаар.

API:
- Mutation route-уудад requireActiveSubscriptionApi нэмэв: orders (POST/PATCH),
  order items (POST + PATCH/DELETE), order payment (PATCH), customers/vehicles/
  services (POST). QPay цуглуулалтын route-уудыг гэйтлээгүй.
- app/api/v1/subscriptions/qpay/callback (шинэ): QPay callback → confirmSubscription
  Payment. createSubscriptionPaymentAction-д callback_url (?payment_id=) нэмэв.
- app/api/v1/subscription (шинэ, GET): нэвтэрсэн staff-ийн багцын төлөв (plan,
  status, locked, daysLeft, expiringSoon, hasPendingPayment) — мобайл клиентэд.

`npm run build` амжилттай (type check OK).

SUBSCRIPTION дуусах сануулга cron нэмэв
---------------------------------------
- prisma: Subscription.reminderSentAt DateTime? нэмж migration
  (20260625000000_add_subscription_reminder_sent_at) apply хийв.
- lib/notifications.ts: "subscription_expiring" notification type (staff realm,
  href → /dashboard/settings/subscription) нэмэв.
- app/api/cron/subscription-reminders (шинэ): SUBSCRIPTION_WARN_DAYS (7) хоногийн
  дотор дуусах TRIAL/ACTIVE багцуудыг олж, тенант бүрийн OWNER-уудад мэдэгдэл +
  push илгээнэ. reminderSentAt-аар давхар илгээхээс сэргийлнэ. CRON_SECRET-ээр
  хамгаалсан (Bearer / ?secret=), бусад cron-той ижил.
- docs/deploy-ubuntu.md: crontab-д өдөр бүр 09:00-д дуудах мөр нэмэв.
`npm run build` амжилттай.

------------------------------------------------------------------------
2026-06-23  ·  commit хийгдээгүй
------------------------------------------------------------------------

BUG FIX: production build TypeScript алдаа — "ITEM_UPDATED" AuditAction
----------------------------------------------------------------------
Захиалгын мөр засах route (app/api/v1/orders/[id]/items/[itemId]/route.ts)
audit бичихдээ "ITEM_UPDATED" action ашигладаг байсан ч энэ утга AuditAction
enum/type-д тодорхойлогдоогүй тул `next build` type check дээр унаж байв.
Засвар (3 газар нэг мөр синк байлгав):
- prisma/schema.prisma — AuditAction enum-д ITEM_UPDATED нэмэв.
- prisma/migrations/20260623000000_add_item_updated_audit_action/migration.sql —
  ALTER TYPE "AuditAction" ADD VALUE 'ITEM_UPDATED' (DB-д applied).
- lib/audit.ts — AuditAction TS union-д "ITEM_UPDATED" нэмэв.
- app/dashboard/audit/page.tsx — ACTION_LABEL ("Мөр зассан") + ACTION_BADGE
  загвар нэмж audit log UI-д зөв харагдана.
Prisma client дахин generate хийж, `npm run build` амжилттай (type check OK).

UI: /showcase bento dashboard демо хуудас нэмэв
-----------------------------------------------
- app/showcase/page.tsx — бие даасан (auth шаардахгүй) bento-grid дашборд.
  Бараан горим, неон violet/blue/emerald/amber онцлол, өндөр контраст.
- Төслийн design token дахин ашиглав: .glass / .gradient-text / .card-hover.
- Гадны dependency байхгүй — бүх чарт (area, sparkline, radial) inline SVG.
- Server component, JS bundle нэмэхгүй. Янз бүрийн хэмжээтэй нүд (2x2, 2x1,
  1x1), өөрийн неон radial-gradient + торон backdrop.


------------------------------------------------------------------------
2026-06-11  ·  commit хийгдээгүй
------------------------------------------------------------------------

DB: QPay urls migration хэрэгжүүлэв
-----------------------------------
- prisma/migrations/20260611000000_add_qpay_urls_to_order_payment — DB-д
  applied хийв (OrderPayment.qpayUrls JSONB null). Өмнө нь schema-д байсан ч
  DB-д хэрэгжээгүй тул /orders/[id]/qpay GET P2022 ColumnNotFound өгч байсан.

BUG FIX: QPay банкны deeplink urls mobile-д ирэхгүй байсан
----------------------------------------------------------
- Шалтгаан: qpayUrls фичерээс өмнө үүссэн pending OrderPayment-ууд qpayUrls=null.
  POST /orders/[id]/qpay нь pending байвал түүнийг дахин ашигладаг тул хуучин
  pending-ийн хувьд `urls: [] ` (хоосон) буцаж байв.
- Засвар:
  · lib/qpay-tenant.ts — getInvoiceUrls(tenantId, invoiceId) нэмэв (QPay
    GET /v2/invoice/{id}-ээс urls дахин татна).
  · app/api/v1/orders/[id]/qpay/route.ts — resolvePaymentUrls() helper:
    хадгалагдсан urls байвал шууд, үгүй бол QPay-аас татаж DB-д нөхөж буцаана.
    GET pending, POST reuse, POST шинэ create гурван замд хэрэглэв (select-д
    qpayInvoiceId нэмэв).



ГОЛ ФИЧЕР: Ажилтны анхны нэвтрэлт — МОБАЙЛ API (шинэ)
-----------------------------------------------------
Веб дэх идэвхжүүлэх урсгалыг (checkLoginEmailAction / requestActivationAction /
activateAccountAction) мобайл API болгон гаргав. Cookie бус, токен (access +
refresh) буцаана.

- lib/auth/api-login.ts (шинэ): buildApiLoginResponse(user, req) — нэвтрэлт
  амжилттай үед буцах нэгдсэн payload (access + refresh token + user).
  /auth/login болон /auth/activate хоёул үүнийг ашиглана (зөрөхгүй).
- app/api/v1/auth/login/route.ts: токен буцаах хэсгийг buildApiLoginResponse-р
  орлуулж рефактор хийв (зан төлөв хэвээр).
- app/api/v1/auth/check-email/route.ts (шинэ): POST { email } →
  status: password | activate | not_registered. activate үед SET_PASSWORD OTP
  илгээж maskedPhone/otpSent буцаана.
- app/api/v1/auth/activate/request-otp/route.ts (шинэ): POST { email } — OTP
  дахин илгээх (enumeration-safe, throttle үед 429).
- app/api/v1/auth/activate/route.ts (шинэ): POST { email, code, password } →
  OTP+нууц үг шалгаж verified=true болгон шууд нэвтрүүлж токен буцаана.
- lib/phone.ts: maskPhone() экспортлов (мобайл route-уудад дахин ашиглах).
- Бүгдэд IP-ийн rate limit (enforceRateLimit) тавьсан.

ГОЛ ФИЧЕР: Ажилтны анхны нэвтрэлт — OTP-ээр идэвхжүүлж нууц үг үүсгэх урсгал
--------------------------------------------------------------------------
Админ ажилтан үүсгэхдээ нууц үг тавихаа больсон. Ажилтан анх удаа өөрийн
утсаар OTP авч, нууц үгээ өөрөө үүсгэж аккаунтаа идэвхжүүлдэг болсон.

- prisma/schema.prisma:
  · User.passwordHash → nullable (String?). Идэвхжих хүртэл null.
  · User.verified (Boolean @default(false)) шинэ талбар нэмэв. false бол
    паролиар нэвтэрч чадахгүй.
  · User.phone → @unique (глобал давхцахгүй, канон 8 оронтой формат).
  · Tenant.name → @unique.
  · OtpType-д SET_PASSWORD утга нэмэв (анхны нэвтрэлтийн нууц үг үүсгэх).

- Migrations (шинэ):
  · 20260610043546_staff_verified_optional_password — verified багана,
    passwordHash NOT NULL хасах, OtpType SET_PASSWORD.
  · 20260610043629_backfill_existing_users_verified — өмнө нь нууц үгтэй
    байсан бүх ажилтныг verified=true болгож backfill (түгжихээс сэргийлэв).
  · 20260610050000_user_phone_unique — User.phone unique index.
  · 20260610051000_tenant_name_unique — Tenant.name unique index.

- app/_actions/auth.ts (+335 мөр):
  · checkLoginEmailAction — имэйлээр төлөв шалгах (нууц үгтэй / идэвхжүүлэх /
    бүртгэлгүй) → нэвтрэх UI салаалуулна.
  · requestActivationAction — идэвхжүүлэх OTP илгээх.
  · activateAccountAction — OTP шалгаж нууц үг үүсгэн аккаунт идэвхжүүлэх.

- app/page/login/login-form.tsx (+250 мөр):
  · Олон алхамт нэвтрэх UI: EmailStep → PasswordStep / ActivateStep /
    NotRegistered. ActivateStep дээр маскласан утас, OTP, шинэ нууц үг.

- app/page/activate/ (шинэ): page.tsx + activate-form.tsx — идэвхжүүлэх хуудас.

- app/api/v1/auth/login/route.ts: !user.verified || !user.passwordHash үед
  нэвтрэлт татгалзах (мобайл API).

- app/dashboard/employees/page.tsx: verified=false ажилтанд "идэвхжээгүй"
  төлөв/тэмдэглэгээ харуулах.

- app/dashboard/employees/employee-form.tsx: нууц үгийн талбар хасч,
  идэвхжүүлэлтийн урсгалд тохируулав.

УТАСНЫ ДУГААРЫН КАНОН НОРМЧЛОЛ (бүх формд нэгтгэв)
-------------------------------------------------
lib/phone.ts-ийн normalizePhone/isValidPhone-г дараах action-уудад нэвтрүүлж,
8 оронтой канон форматаар хадгалж, валидаци хийдэг болгов:
  · app/_actions/profile.ts, tenant.ts, quick-create.ts, employees.ts,
    branches.ts, customers.ts
  · Форм талд (customer-form, vehicle-form, branch-form, signup-form,
    inline-vehicle-form, inline-account-vehicle-form, tenant-form,
    profile-form) оруулга дээр тоо бус тэмдэгт шүүх (replace(/\D+/g,"")).

BUG FIX: Ажилтны утас засахад буруу талбарт алдаа заадаг байсан
----------------------------------------------------------------
- app/_actions/employees.ts (create + update):
  Давхцсан утас оруулахад "имэйл" алдаа заадаг байсан. Шалтгаан:
  @prisma/adapter-pg нь PostgreSQL error.detail-ыг англи regex-ээр задалдаг
  тул lc_messages англи биш орчинд e.meta.target=undefined болж, утасны
  салаа таарахгүй байв. Засвар: meta.target-д найдахаа болиод аль unique
  талбар давхцсаныг DB-ээс шууд лавлах duplicateUserFields() нэмэв.
  ЗАСААГҮЙ: profile.ts, auth.ts, tenant.ts-д ижил pattern үлдсэн.

[2026-06-26] Оношилгооны тайланг PDF болгох (хэвлэх) боломж
  Тайлангийн дэлгэрэнгүй хуудсанд (app/dashboard/diagnostics/reports/[id])
  "PDF татах" товч нэмэв. window.print()-ээр browser-н хэвлэх цонх нээгдэж,
  хэрэглэгч "Save as PDF"-ээр хадгална. Шинэ сан суулгаагүй.
  - print-button.tsx (client) — window.print() дуудах товч.
  - page.tsx — outer div-д id="print-root", header-т товч, footer-т no-print.
  - globals.css — @media print: print-root-оос бусдыг нуух, хар glass
    загварыг цагаан дэвсгэр/хар текст рүү албадах, зураг/гарын үсэг хэвлэх,
    section-уудыг break-inside: avoid.

[2026-06-26] @react-pdf тайланд хоосон гарын үсгийн мөр нэмэв
  pdf-generator.tsx (AdvancedPDFButton) -ийн DiagnosticReportPDF-д
  "Үйлчлүүлэгчийн гарын үсэг" хэсгийг үргэлж гаргадаг болгов: signatureUrl
  байвал зургийг дээр нь, доор нь гараар зурах хоосон мөр (Гарын үсэг /
  Овог нэр) нэмэв. signatureSection/signLine/signCaption styles нэмэв.
  АНХААР: pdf-generator нь Helvetica фонт тул кирилл үсэг харагдахгүй
  (Font.register-ээр Cyrillic TTF бүртгэх хэрэгтэй). AdvancedPDFButton-ийг
  хуудсанд (page.tsx) хараахан холбоогүй — товч гараагүй.

[2026-06-26] @react-pdf PDF: кирилл фонт + товч холболт
  1) Кирилл фонт: public/fonts/-д DejaVuSans.ttf + DejaVuSans-Bold.ttf
     (латин+кирилл, монгол Өө/Үү дэмждэг) нэмж, pdf-generator.tsx-д
     Font.register(family:"DejaVuSans") хийж fontFamily-г Helvetica-аас
     DejaVuSans болгов. Өмнө нь Helvetica кирилл агуулдаггүй тул монгол
     текст хоосон гардаг байв.
  2) AdvancedPDFButton-ийг page.tsx-ийн header-т холбож, report өгөгдлийг
     DiagnosticReportData болгон дамжуулав. Одоо "PDF хүүдүүлэх" товчоор
     жинхэнэ .pdf файл татна.
  АНХААР: @react-pdf доküмент нь зөвхөн header/үйлчлүүлэгч/машин/гарын үсэг
  агуулдаг — оношилгооны section/item-уудыг ОРУУЛААГҮЙ (print хувилбар нь
  бүрэн). Шаардвал DiagnosticReportPDF-д section render нэмэх хэрэгтэй.

[2026-06-26] @react-pdf доküментэд оношилгооны section/item-уудыг нэмэв
  pdf-generator.tsx-ийн DiagnosticReportPDF одоо print хувилбартай ижил
  бүрэн агуулгатай боллоо:
  - DiagnosticReportData-д sections (TemplateSection[]) + data (ReportData)
    нэмэв; page.tsx-аас schema.sections, data дамжуулна.
  - section бүр → item бүр (label, утга, byrlal/positionSet, зураг, item
    гарын үсэг, тэмдэглэл). renderValue + EntryPdf helper (page.tsx-тэй ижил).
  - Үйлчлүүлэгч/машин блокт Бөглөсөн/Гүйлт/Тэмдэглэл мөр нэмэв.
  - lib/diagnostics-аас itemPositions/positionedKey (runtime) + type-уудыг
    импортлов (diagnostic-form.tsx client-д ижил хийдэг тул аюулгүй).
  АНХААР: @react-pdf <Image> нь зураг/гарын үсгийг URL-аас client талд
  татдаг тул Firebase storage CORS зөвшөөрөхгүй бол зураг гарахгүй байж
  магадгүй (print хувилбарт энэ асуудал байхгүй). Дев дээр туршина уу.
